# Khi backend sẵn sàng: đặt URL gốc API (không bắt buộc có / ở cuối).
# Frontend sẽ POST JSON tới: {UMI_APP_API_BASE_URL}/questions
# Để trống hoặc không khai báo = dùng mock (không gọi mạng).
UMI_APP_API_BASE_URL=http://localhost:3000
